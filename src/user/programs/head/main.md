# head

Writes the leading lines of its input. Parses no options — the line count is
fixed — because the shell that drives it has no option-passing conventions
beyond plain argv, and a flag nothing supplies would be untested code.
