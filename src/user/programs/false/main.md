# false

Exits with the status its name implies, and nothing else. Present because a
shell needs them to exercise exit-status handling and conditional
constructs against something with no other behaviour to confuse the result.
