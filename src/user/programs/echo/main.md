# echo

Writes its arguments separated by spaces and a trailing newline. The
simplest real consumer of the argv the spawner encodes, and the one the
shell uses most for redirection tests, since its output is exactly
predictable.
