# Domain service

Role `0x203` owns VM construction and assignment policy and depends on the
process service. This batch establishes its independent PL3 lifecycle and
policy boundary; guest-image loading and the stable VMM API remain open.
