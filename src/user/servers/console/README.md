# Console service

Role `0x202` is ordered after process and device policy and runs as an
unprivileged PL3 service. UART ownership, character buffering, and client IPC
are the next protocol layer and are not claimed by the readiness badge.
