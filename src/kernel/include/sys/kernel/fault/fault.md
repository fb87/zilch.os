# Fault records

User faults are represented as typed records suitable for synchronous delivery to a task's configured fault endpoint. The compatibility fuzz suite uses terminate disposition for the deliberate null fault; the root-task suite may reply with resume or terminate.
