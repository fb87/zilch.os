# Fault records

User faults are represented as typed records suitable for synchronous delivery to a task's configured fault endpoint. The compatibility fuzz profile uses terminate disposition for the deliberate null fault; the root-task profile may reply with resume or terminate.
