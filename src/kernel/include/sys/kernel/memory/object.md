# Memory objects

Frames and page tables are typed kernel objects. Mapping requires capabilities to the target address space and frame. Writable-executable mappings are rejected by the native kernel policy.
