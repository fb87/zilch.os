#include <sys/kernel/capability/cspace.hh>

static_assert(sys::kernel::capability::cspace_slot_count == 256U);
static_assert(sys::kernel::capability::cspace_leaf_count == 4U);
static_assert(sys::kernel::capability::cspace_leaf_slot_count == 64U);
