# Module: arm64 thread/context

Architecture-specific implementation of thread/context. Generic kernel code consumes this through the selected architecture include path and does not reference architecture privilege-level registers or frame layouts directly.
