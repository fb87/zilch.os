# ADR-0002: ARM64 and AMD64 from inception

Accepted. Both architectures are mandatory CI targets so pointer-width, privilege, interrupt, MMU, firmware, and boot assumptions are exposed before subsystem APIs stabilize.
