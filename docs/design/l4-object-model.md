# L4 Object Model

Planned privileged objects: thread, address space, endpoint, notification, capability space, scheduling context, interrupt object, frame, page table, and VM domain. The kernel performs capability-mediated object invocation and synchronous IPC. Pagers and resource managers remain user-space services.
